apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "barebonescrud.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "barebonescrud.name" . }}
    helm.sh/chart: {{ include "barebonescrud.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "barebonescrud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
